{
  "name": "shield_funds",
  "description": "Convert public SOL to private SOL (compress into ZK Compression). Increases the user's private SOL balance.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "amount": {
        "type": "number",
        "description": "Amount of SOL to convert to private"
      }
    },
    "required": ["amount"],
    "additionalProperties": false
  }
}
